# Security policy

Do not report vulnerabilities in public issues. Use the private security contact
configured in the GitHub repository once publication is complete.

This repository never accepts private keys, XRPL seeds, validator tokens,
database credentials, service credentials or production configuration. Rotate
any operational material immediately if it is committed, even briefly.
