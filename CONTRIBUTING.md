# Contributing

Open an issue before substantial design changes. Keep generated and custom code
separate: modify module metadata or templates and regenerate instead of editing
generated output as the final fix.

Before submitting a change, run the backend tests, frontend tests and frontend
build documented in `README.md`. Do not include credentials, runtime ledgers,
validator material or private IDAX artifacts.
